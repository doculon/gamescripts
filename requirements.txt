# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
pyautogui>=0.9.54
Pillow>=10.0.0

# OCR (choose one)
# Option 1: EasyOCR (recommended - better for game fonts)
easyocr>=1.7.0

# Option 2: Pytesseract (lighter, but needs Tesseract installed separately)
# pytesseract>=0.3.10